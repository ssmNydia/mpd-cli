#!/usr/bin/env node
const cli = require('../lib/cli')
const init = require('../lib/init')

cli.setup({
  noArgs: true,
  help: [
    '',
    '  Examples:',
    cli.chalk.gray('    # 初始化 MPD 项目'),
    '    $ mpd init'
  ].join('\n')
})

init()